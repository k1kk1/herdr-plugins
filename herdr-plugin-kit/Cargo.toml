[package]
name = "herdr-plugin-kit"
version = "0.1.0"
edition = "2021"
description = "Shared Herdr socket API client, display naming and picker UI for Herdr plugins"
license = "MIT"

[dependencies]
anyhow = "1"
crossterm = "0.29"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "1"
